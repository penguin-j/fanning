{
  "name": "fanning",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "invoke-local":" serverless invoke local -f send_email_with_file",
    "format": "bundle exec rubocop -a",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "serverless": "^3.32.2"
  },
  "devDependencies": {
    "serverless-ruby-layer": "^1.6.0"
  }
}
